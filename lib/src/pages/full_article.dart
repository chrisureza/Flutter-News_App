import 'package:flutter/material.dart';

class FullArticle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        body: Center(
          child: Text('Full Article Page'),
        ),
      ),
    );
  }
}
